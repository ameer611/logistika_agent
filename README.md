## Installation Guide
1. Clone the repo
2. Enter root folder
3. Create  `.env` file according to `.env.example` file
4. Run the following command:
```bash
docker compose up --build
```
> It takes some time, because it downloads Ollama local model
5. Done, Relax :blush:


## Swagger Docs
http://localhost:8000/docs
